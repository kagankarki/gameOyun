rules_version = '2';

// Firebase Console > Firestore Database > Kurallar (Rules) sekmesine yapıştırıp Yayınla (Publish) deyin.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
